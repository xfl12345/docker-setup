services:
  clash:
    extends:
      file: ./docker-compose.morefree.example.yml
      service: clash
    network_mode: host
  metacubexd:
    extends:
      file: ./docker-compose.morefree.example.yml
      service: metacubexd
    network_mode: bridge
