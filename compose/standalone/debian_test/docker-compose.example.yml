services:
  debian_test:
    extends:
      file: ./docker-compose.morefree.example.yml
      service: debian_test
    networks:
      my_docker_network: {}

include:
  - ../../docker-compose.preset.network.yml
